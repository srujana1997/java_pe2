package com.stackroute.pe2;
public class EvenNumTest {
    // isEven() is a boolean method which takes an int as input and returns true if the input is even.
        public static boolean isEven(int number)
        {
            return number % 2 == 0;
        }
}
